<h1>Kiste Wurde Gezahlt</h1>

<%= form_for(@crate) do |f| %>
  <% if @crate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@crate.errors.count, "error") %> prohibited this crate from being saved:</h2>

      <ul>
      <% @crate.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label 'Wessen: ' %><%= @crate.cratepayer.name %>
  </div>

  <div class="field">
    <%= f.label 'Warum: ' %><%= @crate.reason %>
  </div>

  <div class="field">
    <%= f.label 'Wann: ' %><%= @crate.date %>
  </div>

  <div class="field">
    <%= f.label 'Wann Bezahlt' %><br />
    <%= f.date_select :paid %>
  </div>

  <div class="actions">
    <%= f.submit 'Gezahlt' %>
  </div>
<% end %>


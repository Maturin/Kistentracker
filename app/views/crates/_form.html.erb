<%= form_for(@crater) do |f| %>
  <% if @crater.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@crater.errors.count, "error") %> prohibited this crater from being saved:</h2>

      <ul>
      <% @crater.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label 'Wer' %><br />
    <%= f.select :craterpayer_id, @payers %>
  </div>
  <div class="field">
    <%= f.label 'Warum' %><br />
    <%= f.text_field :reason %>
  </div>
  <div class="field">
    <%= f.label 'Wann' %><br />
    <%= f.date_select :date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
